Computations with distributed matrices gather and scatter operations

transporttestpar1 uses labsend and labreceive to distribute a matrix
 perform computation on different part of each matrix
 gather the matrix onto one processor

transportestpar2 uses matlab distributor to distribute a matrix across processors
 gather is used to collect each component